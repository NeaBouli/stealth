package com.securecall.app.ghostnet.frame.body

/**
 * CRYPTO-38:
 * AudioBodyParser v1 (Stub).
 * Später kommt hier Opus / PCM / Jitterbuffer / Timestamping rein.
 */
object AudioBodyParser {
    fun parse(body: ByteArray): ByteArray {
        return body // Stub: unverändert
    }
}
