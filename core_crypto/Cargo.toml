[package]
name = "securecall_core_crypto"
version = "0.1.0"
edition = "2021"
description = "Core Crypto Engine for SecureCall (Rust, CRYPTO-02 skeleton)"
license = "MIT OR Apache-2.0"
authors = ["SecureCall Team"]

[lib]
name = "securecall_core_crypto"
crate-type = ["cdylib", "rlib"]

[dependencies]
rand = "0.8"
